# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

This is a ticketing SaaS project in its initial state. No source code, build tooling, or framework has been added yet.

When the stack is chosen and code is added, update this file with:
- Build, lint, and test commands (including how to run a single test)
- Architecture overview: how the layers connect (e.g. API → service → data layer), where auth lives, how multi-tenancy is handled
- Any non-obvious conventions specific to this codebase
