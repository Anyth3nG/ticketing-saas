DATABASE_URL=postgresql://ticketing_dev:your_password@localhost/ticketing_saas
CLERK_SECRET_KEY=sk_test_...
ENVIRONMENT=development

# Dev-only: clerk_id of the DB user that get_current_user resolves to until
# real Clerk JWT validation is wired in. Must match a seeded users.clerk_id row.
DEV_USER_CLERK_ID=dev_stub_clerk_id
