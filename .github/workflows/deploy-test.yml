name: Test

# A push to staging BUILDS and publishes images but does NOT deploy them.
# Deploying is an explicit manual run from the Actions tab.
#
# The test instance is normally STOPPED to save cost, so a deploy here starts
# with someone starting it. That is also why deploying is manual rather than
# automatic: a push would otherwise fail against a box that is switched off.
#
# AT CUTOVER: delete the `if:` on the deploy job.
on:
  push:
    branches: [staging]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build:
    name: Build and push images
    runs-on: ubuntu-latest
    # Every per-environment secret and variable now lives INSIDE this
    # GitHub Environment, and a job only sees those if it names the
    # environment. Without this line they resolve to an empty string --
    # no error, no warning. That is how a prod deploy came to run
    # `aws s3 sync ... s3://` and `scp ubuntu@:` on 2026-09-06.
    #
    # The BUILD job needs it as much as the deploy: it reads the Clerk
    # publishable key, which Vite inlines at build time. Miss it here and
    # the build stays green while shipping a bundle nobody can log into.
    environment: test
    # The CRM's test address, for the navbar link that opens it signed in (see
    # frontend/.env.example). A literal, like the CRM hostname in the deploy
    # job below. The assert step reads it from here too.
    env:
      VITE_CRM_URL: https://api-testing.max-cpa.co.il
    permissions:
      contents: read
      packages: write
    outputs:
      backend_image: ${{ steps.meta.outputs.backend_image }}
      frontend_image: ${{ steps.meta.outputs.frontend_image }}
    steps:
      - uses: actions/checkout@v4

      - name: Compute image names
        id: meta
        run: |
          owner=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          base="${REGISTRY}/${owner}/ticketing-saas"
          echo "backend_image=${base}-backend:${{ github.sha }}" >> "$GITHUB_OUTPUT"
          echo "frontend_image=${base}-frontend:${{ github.sha }}" >> "$GITHUB_OUTPUT"

      - name: Assert the frontend build args are present
        env:
          VITE_CLERK_PUBLISHABLE_KEY: ${{ vars.VITE_CLERK_PUBLISHABLE_KEY }}
        run: ./deploy/render-env.sh frontend/.env.example > /dev/null

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: ${{ steps.meta.outputs.backend_image }}

      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: ${{ steps.meta.outputs.frontend_image }}
          build-args: |
            VITE_CLERK_PUBLISHABLE_KEY=${{ vars.VITE_CLERK_PUBLISHABLE_KEY }}
            VITE_CRM_URL=${{ env.VITE_CRM_URL }}

  deploy:
    name: Deploy to test
    needs: build
    runs-on: ubuntu-latest
    environment: test
    # DELETE THIS LINE AT CUTOVER.
    if: github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - name: Render backend.env
        env:
          # EVERY key in backend/.env.example must appear here, or the build
          # fails. See render-env.sh for why that matters on this project.
          # DATABASE_URL is BUILT from the same two secrets that create the
          # database, never stored as a URL of its own. It used to be
          # secrets.DATABASE_URL_TEST, which carried a second copy of the role
          # and password that POSTGRES_USER_TEST/POSTGRES_PASSWORD_TEST already
          # supply -- and two copies of one credential duly drifted apart. The
          # stored URL still said `localhost` months after the database moved
          # into a container, and still named a role the volume did not have;
          # nothing could compare them because only one of the two was ever
          # read by the app. Derived here, they cannot disagree.
          #
          # The host is `postgres`, the compose service name, resolved on the
          # compose network. It is a fact about the topology rather than a
          # secret, so it belongs in the open where a reviewer can see it --
          # being hidden inside a secret is precisely how `localhost` survived
          # the move off bare metal.
          DATABASE_URL: postgresql://${{ secrets.POSTGRES_USER_TEST }}:${{ secrets.POSTGRES_PASSWORD_TEST }}@postgres:5432/ticketing_saas
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          CLERK_FRONTEND_API: ${{ secrets.CLERK_FRONTEND_API }}
          ENVIRONMENT: test
          ADMIN_EMAIL: ${{ vars.ADMIN_EMAIL }}
          MANAGER_EMAIL: ${{ vars.MANAGER_EMAIL }}
        run: ./deploy/render-env.sh backend/.env.example > /tmp/backend.env

      - name: Render the compose .env
        env:
          # The same contract as backend.env above: every key in the template
          # must have a non-empty value here, or the build fails naming the
          # ones that do not.
          #
          # This replaces a fixed heredoc, which is how the now-removed
          # CRM_DB_USER once reached the test box as an empty string -- the
          # GitHub variable was never set, the heredoc wrote `CRM_DB_USER=`
          # regardless, and nothing reported it. Postgres reads values like
          # that exactly once, on a first `up` against an empty volume, so
          # there is no second chance to notice.
          #
          # COMPOSE_FILE travels in this file so a bare `docker compose` in
          # ~/stack finds the stack. See deploy/compose.env.example.
          COMPOSE_FILE: docker-compose.prod.yml
          POSTGRES_USER: ${{ secrets.POSTGRES_USER_TEST }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD_TEST }}
          BACKEND_IMAGE: ${{ needs.build.outputs.backend_image }}
          FRONTEND_IMAGE: ${{ needs.build.outputs.frontend_image }}
        run: ./deploy/render-env.sh deploy/compose.env.example > /tmp/compose.env

      - name: Assemble the stack directory
        run: |
          mkdir -p /tmp/stack
          cp -r deploy/. /tmp/stack/
          cp -r proxy/deploy/conf.d /tmp/stack/proxy-templates
          mv /tmp/backend.env /tmp/stack/backend.env
          # Compose reads .env by default, which is what lets deploy.sh and a
          # human in ~/stack run the identical bare `docker compose`.
          mv /tmp/compose.env /tmp/stack/.env
          chmod 600 /tmp/stack/backend.env /tmp/stack/.env
          chmod +x /tmp/stack/*.sh

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 400 ~/.ssh/id_rsa

      - name: Copy the stack to the box
        run: |
          ssh -o StrictHostKeyChecking=no ${{ vars.EC2_USER }}@${{ vars.EC2_HOST_TEST }} "mkdir -p ~/stack"
          scp -o StrictHostKeyChecking=no -r /tmp/stack/. \
            ${{ vars.EC2_USER }}@${{ vars.EC2_HOST_TEST }}:~/stack/

      # A SEPARATE STEP, AND IT HAS TO STAY ONE.
      #
      # Installs Docker if the box has none. It cannot be chained ahead of the
      # docker login below, because bootstrap.sh adds the deploy user to the
      # `docker` group and group membership is resolved at login -- the session
      # that ran usermod does not have it. Same ssh call would install Docker
      # and then fail on the socket. A new step is a new session.
      - name: Prepare the box
        run: |
          ssh -o StrictHostKeyChecking=no ${{ vars.EC2_USER }}@${{ vars.EC2_HOST_TEST }} "
            bash ~/stack/bootstrap.sh '${{ vars.EC2_USER }}'
          "

      # Test's hostnames, decided 2026-09-10: TEST_DOMAIN (testing) is this app,
      # and api-testing is the CRM's, handed across to the CRM's stack by this
      # proxy (proxy/deploy/conf.d/crm-http.conf).
      #
      # So test has NO legacy API hostname -- `none`. That retires the
      # api-testing block the old S3 bundle needed, and nothing can load that
      # bundle any more: testing's DNS stopped pointing at the bucket the same
      # day. VITE_API_URL is no longer read here for the same reason.
      #
      # The CRM hostname is a literal, like the database host above: a fact
      # about the topology, visible to a reviewer, unable to arrive empty.
      #
      # DEPLOY THE CRM FIRST if its proxy still binds the host's :80, as it did
      # from 2026-09-07 to 2026-09-10. Otherwise `up` fails on the port.
      - name: Deploy
        run: |
          ssh -o StrictHostKeyChecking=no ${{ vars.EC2_USER }}@${{ vars.EC2_HOST_TEST }} "
            set -e
            echo '${{ secrets.GHCR_PULL_TOKEN }}' | docker login ghcr.io -u '${{ github.actor }}' --password-stdin
            bash ~/stack/deploy.sh '${{ vars.TEST_DOMAIN }}' none '${{ vars.CERTBOT_EMAIL }}' api-testing.max-cpa.co.il
          "

      - name: Smoke test
        run: |
          for i in $(seq 1 10); do
            if curl -fsS "https://${{ vars.TEST_DOMAIN }}/health"; then
              echo " <- test healthy"; exit 0
            fi
            sleep 6
          done
          echo "health check never passed" >&2; exit 1
